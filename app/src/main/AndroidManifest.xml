<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.duck.fastnotes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".App"
        android:theme="@style/Theme.FastNotes">

        <activity
            android:name=".features.welcome.WelcomeScreenActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.FastNotes.NoActionBar">
        </activity>

        <activity
            android:name=".features.dashboard.DashboardActivity"
            android:exported="true"
            android:theme="@style/Theme.FastNotes.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>